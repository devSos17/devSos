---
interface Props {
	title: string;
	body: string;
	href: string;
	class?: string;
}

const { href, title, body, class: classes } = Astro.props;
---

<li class={classes}>
	<a href={href}>
		<h2>
			{title}
			<span>&rarr;</span>
		</h2>
		<p>
			{body}
		</p>
	</a>
</li>
